---
import ArrowLeft from '../assets/images/arrow-left.svg';

interface Props {
    title: string;
    medium: string;
    Dimensions: {
        width: number;
        height: number;
    };
    year: string;
    className?: string;
    links?: {
        previous: string;
        next: string;
    };
    id?: string;
}

const {
    title,
    medium,
    Dimensions: { width, height },
    year,
    links,
    className,
    id = '',
} = Astro.props;

const fullyear = new Date(year).getFullYear();
---

<style>
    .links {
        display: flex;
        justify-content: space-between;
        margin-top: 1rem;
        max-width: 200px;
    }
    .caption {
        display: flex;
        flex-direction: column;

        /* margin-top: 1rem; */
        line-height: 1.5rem;
    }

    .right {
        transform: rotate(180deg);
    }
</style>

<div {id} class={`caption ${className || ''}`}>
    <h2>
        <span class="font-bold">{title}</span>, {fullyear}
    </h2>
    <span class="">{medium}</span>
    {width} x {height} cm
    {
        links && (
            <div class="links">
                <a href={links.previous} title="Previous">
                    <ArrowLeft class="stroke-gray-500 hover:stroke-amber-900" width="auto" height="1.5rem" />
                </a>
                <a href={links.next} title="Next">
                    <ArrowLeft class="right stroke-gray-500 hover:stroke-amber-900" width="auto" height="1.5rem" />
                </a>
            </div>
        )
    }
</div>
